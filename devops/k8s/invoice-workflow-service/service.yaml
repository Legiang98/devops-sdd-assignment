apiVersion: v1
kind: Service
metadata:
  name: invoice-workflow-service
  namespace: devops-ssd-assignment
spec:
  selector:
    app: invoice-workflow-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
