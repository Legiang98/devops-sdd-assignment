[project]
name = "spec-driven-devops-poc"
version = "0.1.0"
description = "Spec-driven DevOps PoC with agent + policy gate + observability"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.116.0",
  "uvicorn>=0.35.0",
  "pydantic>=2.11.0",
  "pyyaml>=6.0.2",
  "prometheus-client>=0.22.1",
  "opentelemetry-sdk>=1.27.0",
  "opentelemetry-exporter-otlp-proto-http>=1.27.0",
  "opentelemetry-instrumentation-fastapi>=0.48b0",
]

[tool.uv]
dev-dependencies = []
